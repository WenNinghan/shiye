{
  "name": "shiye-desktop",
  "productName": "识页",
  "version": "0.3.0",
  "private": true,
  "license": "MIT",
  "description": "识页 · 本地 OCR、公式校对与文档转换",
  "author": "AIAADC",
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.cjs",
    "test:desktop": "playwright test -c playwright.config.cjs",
    "pack": "electron-builder --config electron-builder.config.cjs --dir --win --x64",
    "dist": "electron-builder --config electron-builder.config.cjs --win nsis --x64"
  },
  "dependencies": { "yauzl": "3.2.0" },
  "devDependencies": {
    "electron": "44.4.1",
    "electron-builder": "26.15.3",
    "@playwright/test": "1.58.2"
  }
}
