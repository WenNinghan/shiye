# 分发材料阶段验收（G3）

2026-09-16。此记录只验收本轮新增的收集工具、清单及调查结果；不宣称桌面公开发行目标已完成。

| 原计划验收项 | 验证方式与结果 | 状态 |
| --- | --- | --- |
| 模型身份 | 重新计算两个 inference.pdiparams 的 SHA-256，与模型来源记录一致 | 通过该子项，非整个模型包通过 |
| Python 物料 | 用各自实际构建解释器读取对应 Analysis，得到核心 35 / 公式 103 项；未映射 site-packages 文件 0 | 完成首轮，原生依赖和元数据仍需人工区分 |
| 原始许可 | 按包文件及许可证目录收集；修正仅按文件名导致漏掉 PDFium 许可证目录的问题；保留缺文本状态 | 部分完成，不用 UNKNOWN 自动判定许可 |
| Python 对应源码 | 118 个名称/版本组合，113 个 sdist 已下载并验证大小与 SHA-256；5 个无 sdist 明示记录 | 部分完成 |
| MuPDF / GEOS | 另下载完整 MuPDF 1.28.2 和 GEOS 3.13.1 原生源码归档并记录 SHA-256；MuPDF 包内有 thirdparty 源码树 | 完成这两个归档获取，不等于所有对应源码齐备 |
| 收集器回归 | `python -m unittest discover -s packaging/tests -v`，6/6 通过 | 通过；未下载或执行测试中的第三方代码 |
| 组合分发兼容性 | 实际 OpenCV 构建信息、wheel 附带条款、锁定源码的 IPP 下载配置、实际 IPP 归档 EULA 交叉核实 | 阻断：含静态 IPP，待处理无 IPP 构建路线 |
| 许可 UI、新包及模型哈希 | 因上述真实阻断未进入最终打包，保留原文件 | 未实施，不伪造新资产或验证结果 |
| 公开 Release | 尚未上传安装器、模型包或第三方原始二进制 | 未完成 |

保留项目自有 MIT、旧内测包、开发环境和用户数据。没有安装本机 MSVC、购买商业许可、调用收费 API，也没有清理 C 盘。临时及下载资料位于独立 G 盘构建目录。

新增可复用入口已登记原开发工作区能力地图。G2 与本验收归档到本地 `docs/历史档案/任务归档/`；该目录按现有 `.gitignore` 不公开，仓库中的本文件为可引用的验收入口。

下一步需要作者确认新构建范围，而不是重新确认已经同意的 MIT/AGPL 路线：建议 GitHub Actions Windows 构建锁定版本、无 IPP 的 OpenCV wheel，再按原计划继续材料覆盖和完整程序回归。未经确认不新增 CI、安装大型编译工具链或切换 PDF 引擎。
